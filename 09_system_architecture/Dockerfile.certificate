FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install fastapi uvicorn psycopg2-binary redis pika minio

# Copy application code
COPY certificate_service.py .

# Run the application
CMD ["uvicorn", "certificate_service:app", "--host", "0.0.0.0", "--port", "8000"]
